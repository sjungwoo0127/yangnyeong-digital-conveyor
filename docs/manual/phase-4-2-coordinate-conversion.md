# 좌표 변환 (Pixel to Robot Conversion)

## 🔄 개요

카메라에서 감지한 물체의 픽셀 좌표를 로봇이 이해할 수 있는 공간 좌표로 변환합니다.

---

## 📐 좌표 변환 과정

### 카메라 좌표 vs 로봇 좌표

**카메라 이미지** (픽셀 단위)
```
┌─────────────┐
│     ●       │ (pixel_x, pixel_y)
│             │ 범위: 0~320, 0~240
└─────────────┘
```

**로봇 공간** (mm 단위)
```
로봇 팔의 끝(흡입기)이 도달할 위치
(robot_x, robot_y, robot_z)
```

---

## 🖥️ 변환 함수

```python
def pixel_to_suction_movement(pixel_x, pixel_y, object_height=22):
    """
    픽셀 좌표 → 로봇 좌표 변환
    
    입력:
    - pixel_x, pixel_y: 카메라 이미지 픽셀 좌표
    - object_height: 물체 높이
    
    출력:
    - (robot_x, robot_y): 로봇 팔의 위치
    """
    
    # 복잡한 수학적 변환:
    # 1. 픽셀을 mm로 변환
    # 2. 카메라 높이에 따른 보정
    # 3. 로봇 각도에 따른 보정
    # 4. 최종 로봇 좌표 계산
    
    return robot_x, robot_y
```

---

## 📊 변환 파라미터

| 파라미터 | 값 | 설명 |
|---------|-----|------|
| `camera_height` | 93.7mm | 카메라 높이 |
| `suction_height` | 40mm | 흡입기 높이 |
| `suction_offset_x` | -28mm | X축 오프셋 |
| `suction_offset_y` | -16mm | Y축 오프셋 |

---

## 💡 변환 과정 (간단 설명)

1. **픽셀 → mm 변환**
   - 카메라가 보는 실제 범위: 약 65mm × 60mm
   - 이를 320×240 픽셀로 나눔

2. **높이 보정**
   - 로봇이 높을수록 보이는 범위가 달라짐
   - 높이에 따라 스케일 조정

3. **로봇 각도 보정**
   - 로봇이 바라보는 각도에 따라 방향 조정
   - 삼각함수로 계산

4. **최종 좌표 계산**
   - 모든 보정을 적용하여 로봇이 집어야 할 위치 결정

---

## ✅ 변환 확인

물체가 정확히 집혀지지 않으면:
1. 카메라 각도 확인
2. Phase 3 카메라 설정 재확인
3. 파라미터 `suction_offset_x/y` 미세 조정

---

**뒤로**: [Phase 4: 물체 분류](phase-4-classify.md)
